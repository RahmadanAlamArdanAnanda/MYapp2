<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MYapp">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".POLICE"></activity>
        <activity android:name=".PolsekBKN"></activity>
        <activity android:name=".PolsekTMPN"></activity>
        <activity android:name=".PolsekTR"></activity>
        <activity android:name=".PolsekMDAU"></activity>
        <activity android:name=".PolsekBKTRYA"></activity>
    </application>

</manifest>